import 'package:flutter/cupertino.dart';

class ContactProfileWeb extends StatelessWidget {
  const ContactProfileWeb({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
