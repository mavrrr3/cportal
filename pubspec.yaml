name: cportal_flutter
description: Crossplatform corparative portal.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.6 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.2

  # Dependency Injection tool
  get_it: ^7.2.0

  # Remote API
  dio: ^4.0.6
  internet_connection_checker: ^0.0.1+3

  # Functional programming things
  dartz: ^0.10.1

  # Value equality
  equatable: ^2.0.3

  # Key-value local cache/db
  hive: ^2.2.2
  hive_flutter: ^1.1.0

  # Localization
  intl: ^0.17.0

  # Theme
  adaptive_theme: ^3.1.0
  flutter_svg: ^1.1.1+1
  google_fonts: ^2.3.1
  pinput: ^2.2.9

  # State management for realisation bisness logic
  bloc_concurrency: ^0.2.0
  flutter_bloc: ^8.1.1

  # Other
  device_info_plus_web: ^2.1.0
  go_router: ^4.1.0
  local_auth: ^1.1.11
  flutter_dotenv: ^5.0.2
  swipe: ^0.0.1
  fluttertoast: ^8.0.9
  url_launcher: ^6.1.5
  qr_code_scanner:
    git:
      url: https://github.com/xeinebiu/qr_code_scanner.git
      ref: fix_break_changes_platform
  flutter_vibrate: ^1.3.0
  expandable: ^5.0.1

  # Lottie is animations library
  lottie: ^1.4.2
  loading_indicator: ^3.1.0

  # Image Cache package
  extended_image: ^6.2.1
  

  responsive_framework: ^0.2.0
  qr_flutter: ^4.0.0
  bloc_test: ^9.0.3

  json_annotation: ^4.5.0
 
  device_info_plus: ^4.0.0
  expand_tap_area: ^1.1.1



dev_dependencies:
  dart_code_metrics: ^4.16.0
  flutter_lints: ^1.0.0
  l: ^3.2.0
  hive_generator: ^1.1.3
  flutter_launcher_icons: ^0.9.2
  build_runner: ^2.1.8
  json_serializable: ^6.2.0

  # Testing packages
  flutter_test:
    sdk: flutter
  mockito: ^5.1.0
  mocktail: ^0.3.0
  test: ^1.19.5



flutter:
  generate: true
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/
    - assets/lottie/
    - assets/icons/
    - assets/img/
    - assets/img/onboarding/
    - assets/icons/navbar/
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages
