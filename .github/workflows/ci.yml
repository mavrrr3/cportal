name: CI
on:
  pull_request:
    branches:
      - dev

jobs:
  flutter_test:
    name: Run dart-code metrics analyze and flutter analize
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v1
        with:
          channel: "stable"
      - run: flutter pub get
      - run: flutter analize
      - run: flutter pub run dart_code_metrics:metrics analyze lib
